# @kbn/as-code-shared-constants

Shared constants used across Kibana as-code schema and transform packages.

## Usage

```ts
import {
  ASCODE_FILTER_OPERATOR,
  ASCODE_FILTER_TYPE,
  AS_CODE_DATA_VIEW_REFERENCE_TYPE,
} from '@kbn/as-code-shared-constants';
```

